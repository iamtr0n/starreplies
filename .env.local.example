# StarReplies - Environment Variables
# Copy this to .env.local and fill in values

# Admin password (change this before going to production)
ADMIN_PASSWORD=gridadmin2026

# Data directory (defaults to /data/signups.json in project root)
# DATA_DIR=./data
